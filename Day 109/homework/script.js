//Codecademy

//1) კომენტარის სახით ახსენით თითოეული მეთოდი რომელც დღეს გავიარეთ 

//2) კომენტარის სახით ახსენით რაში და როგორ გამოიყენება დღეს ნასწავლი ფუნქციები map'ი და filter'ი

//3) შექმენით სია სადაც შეინახავთ თქვენთვის სასურველ 10 რიცხვს შემდეგ მაპის მეშვეობით გამოიტანეთ ლუწ ინდექსზე მდგომი ელემენტების ნამრავლი ხოლო კენტ ინდექსზე მდგომ რიცხვებს დაუმატეთ 5 და დააუბრენეთ მიღებული პასუხი

///4) შექმენით სახელების სია და მაპის მეშვეობით გადაუარეთ ამ სიას შემდეგ კი გამოიტანეთ ჯერ ამ სიის ინდექსები შემდეგ კი ამ სიაში მყოფი ელემენტები 

//5) ზემოთ შექმნილი რიცხვების სიას გადაუარეთ filter'ის მეშვეობით და შეამოწმეთ თუ მოცემული ელემენტის 2 ზე გაყოფისას მიღებული რიცხვი მეტია 20 ზე მაშინ დააბრუნეთ true სხვა შემთხვევაში false 

///6) map ფუნქციასა და filter ფუნქციაზე გააკეთეთ 5-5 მაგალითი დღეს ნასწავლი მეთდების გამოყენებით და თითოეული მაგალითი დაწვრილებით ახსენით კომენტარის სახით



//1)
//  map() — გამოიყენება მასივის თითოეულ ელემენტზე გადასასვლელად და მისი გარდასაქმნელად.
// აბრუნებს ახალ მასივს, რომლის სიგრძეც იგივეა, რაც ორიგინალის.

//  filter() — ასევე გადადის მასივის თითოეულ ელემენტზე,
// მაგრამ აბრუნებს მხოლოდ იმ ელემენტებს, რომლებიც აკმაყოფილებენ პირობას (true).

// forEach() — გადადის მასივზე, მაგრამ არაფერს აბრუნებს,
// ძირითადად გამოიყენება რაიმე ქმედების შესასრულებლად (მაგ. კონსოლში დაბეჭდვა).

// reduce() — გამოიყენება მასივის ელემენტების შესაჯამებლად ერთ მნიშვნელობამდე
// (მაგალითად ელემენტების ჯამის ან ნამრავლის გამოთვლა).

//  find() — აბრუნებს პირველ ელემენტს, რომელიც აკმაყოფილებს პირობას.

//  some() — აბრუნებს true-ს თუ მასივში მაინც ერთი ელემენტი აკმაყოფილებს პირობას.

//  every() — აბრუნებს true-ს მხოლოდ მაშინ, როცა ყველა ელემენტი აკმაყოფილებს პირობას.


//2)
//  map() — გამოიყენება, როცა გვინდა მასივის თითოეული ელემენტის შეცვლა ან გარდაქმნა.
// მაგალითად: თითოეულ რიცხვს გავუზარდოთ მნიშვნელობა 2-ით.

const nums = [1, 2, 3, 4];
const doubled = nums.map(num => num * 2)

//  filter() — გამოიყენება, როცა გვინდა მასივიდან ამოვფილტროთ ელემენტები გარკვეული პირობით.
// მაგალითად: მხოლოდ ლუწი რიცხვების ამოღება.

const evenNums = nums.filter(num => num % 2 === 0)


//3)

const numbers = [3, 12, 45, 7, 22, 9, 30, 14, 55, 2];

const result = numbers.map((num, index) => {
  if (index % 2 === 0) {
    return num * num; 
  } else {
    return num + 5;  
  }
});

console.log(result);

//4)
const names = ["giorgi", "nika", "sopo", "tamari", "alex"];

names.map((name, index) => {
  console.log(`index: ${index} element: ${name}`);
});


//5)
const filtered = numbers.filter(num => {
  if ((num / 2) > 20) {
    return true;
  } else {
    return false;
  }
});

console.log(filtered);


//6)

//map

// 1 
const plusTen = numbers.map(n => n + 10);

// 2️ 
const doubledNums = numbers.map(n => n * 2);

// 3️
const textNums = numbers.map(n => `${n}`);

// 4️ 
const evenOdd = numbers.map(n => n % 2 === 0 );

// 5️
const indexed = numbers.map((n, i) => `indx ${i}: ${n}`);


//filter
// 1️ 
const even = numbers.filter(n => n % 2 === 0);

// 2️ 
const odd = numbers.filter(n => n % 2 !== 0);

// 3️ 
const overTwenty = numbers.filter(n => n > 20);

// 4️ 
const underTen = numbers.filter(n => n < 10);
